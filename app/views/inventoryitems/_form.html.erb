<%= form_for(@inventoryitem) do |f| %>
  <% if @inventoryitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventoryitem.errors.count, "error") %> prohibited this inventoryitem from being saved:</h2>

      <ul>
      <% @inventoryitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_name %><br />
    <%= f.text_field :item_name %>
  </div>
  <div class="field">
    <%= f.label :branch_id %><br />
    <%= f.text_field :branch_id %>
  </div>
  <div class="field">
	<%= f.collection_select :unit_id, Unit.find(:all), :id, :unit_name, :prompt => "Select unit" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
